# ---------------------------------------------------------------------
# 1. Gemini API Key (Required for all ADK Agents)
# ---------------------------------------------------------------------
GEMINI_API_KEY="YOUR_ACTUAL_GEMINI_API_KEY_HERE"

# ---------------------------------------------------------------------
# 2. Database Connection Credentials (For database_manager.py)
# ---------------------------------------------------------------------
# Replace these with your local or cloud MySQL credentials
DB_HOST="127.0.0.1"
DB_PORT="3306"
DB_NAME="arch_pipeline_db"
DB_USER="arch_admin"
DB_PASSWORD="A_Secure_Random_Password_123"

# ---------------------------------------------------------------------
# 3. Project Configuration (For ADK Agents and Workflow)
# ---------------------------------------------------------------------
# The default project ID the pipeline will load if not specified by the user
DEFAULT_PROJECT_ID="ECOMMERCE-V1"

# The name of the user/system initiating the session
INITIATOR_USER_ID="System-Admin"

# ---------------------------------------------------------------------
# 4. PlantUML/Diagram Configuration (Future Use)
# ---------------------------------------------------------------------
# Optional: Path to the PlantUML executable if running locally
PLANTUML_JAR_PATH="/path/to/plantuml.jar"